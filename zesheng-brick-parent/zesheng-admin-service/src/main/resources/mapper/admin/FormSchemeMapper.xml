<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zesheng.admin.mapper.FormSchemeMapper">

    <!-- 自定义 insert：显式 useGeneratedKeys 回填 id，并用 NOW() 填充 created_at、updated_at。keyProperty 必须用 entity.id 以正确绑定到继承自 BaseEntity 的 id 字段 -->
    <insert id="insertAndFillId" useGeneratedKeys="true" keyProperty="entity.id" keyColumn="id">
        INSERT INTO admin_form_schemes (name, address_id, description, status, unit_price, created_at, updated_at)
        VALUES (#{entity.name}, #{entity.addressId}, #{entity.description}, #{entity.status}, #{entity.unitPrice}, NOW(), NOW())
    </insert>

</mapper>
